{% extends 'base.html.twig' %}

{% block title %} Modifier la sortie {% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col d-flex justify-content-center">
                <p> </p>
            </div>
        </div>
        <div class="row">
            <div class="col d-flex justify-content-center">
                <p> </p>
                <h1 class="h3 mb-3 font-weight-normal">Modifier une sortie</h1>
            </div>

        </div>
        <br>
        <div class="row">

            <div class="col-12 d-flex justify-content-center">
                <div class="card" style="width: 28rem;">
                    <div class="card-body">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}

                        <i style="font-size: small">* Ce champ ne doit pas être vide</i><br>
                        <br>
                        <button type="submit" class="btn btn-light" name="createSortie" value="Créée" onclick=window.location.href="{{ path('creer') }}>Enregistrer</button>
                        <button type="submit" class="btn btn-outline-warning" name="createSortie" value="Ouverte">Publier</button>
                        {{ form_end(form) }}

                    </div>
                </div>
            </div>
        </div>
        <br>
        <br>
        <br>
    </div>




























{#    <h2>Modification de la sortie</h2>#}
{#    <br>#}
{#    {{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}

{#    <i style="font-size: small">* Ce champ ne doit pas être vide</i><br>#}
{#    <br>#}
{#    <button type="submit" class="btn btn-light" name="createSortie" value="Créée" onclick=window.location.href="{{ path('creer') }}>Enregistrer</button>#}
{#    <button type="submit" class="btn btn-light" name="createSortie" value="Ouverte">Publier</button>#}
{#    {{ form_end(form) }}#}



{% endblock %}




{% extends 'base.html.twig' %}

{% block title %}Reset | {{ parent() }}{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col d-flex justify-content-center">
                <p></p>
            </div>
        </div>
        <div class="row">
            <div class="col d-flex justify-content-center">
                <p></p>
                <h1 class="h3 mb-3 font-weight-normal">Réinitialise ton mot de passe</h1>
            </div>
        </div>


        <div class="row">
            <div class="col d-flex justify-content-center">

                <div class="card" ">
                <div class="card-body">

                    {{ form_start(resetForm) }}
                    {{ form_row(resetForm.plainPassword) }}
                    <button class="btn btn-secondary btn-sm">changer mot de passe</button>
                    {{ form_end(resetForm) }}
                </div>
            </div>
        </div>
    </div>
    </div>

{% endblock %}
